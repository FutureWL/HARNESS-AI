services:
  harness-portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "37210:80"
      - "37211:443"
    volumes:
      - ./certs:/etc/nginx/certs
    restart: unless-stopped
